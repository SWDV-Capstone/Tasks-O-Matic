import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tasks-O-Matic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
